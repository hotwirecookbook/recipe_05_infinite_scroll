<%= turbo_stream.append "tweets", partial: "tweets/tweets", locals: { tweets: @tweets } %>
 
<% if @pagy.next.present? %>
  <%= turbo_stream.replace "infinite_scroll",
    partial: "tweets/turbo_frame",
    locals: { page: @pagy.next } %>
<% end %>